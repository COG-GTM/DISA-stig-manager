---
# Handlers for session security role

- name: Reload dconf
  ansible.builtin.command:
    cmd: dconf update
  changed_when: true

- name: Restart systemd-logind
  ansible.builtin.systemd:
    name: systemd-logind
    state: restarted
